The shell can be used in interactive mode or non-interactive mode.

.SH "INTERACTIVE MODE"
To start the shell in interactive mode:

.nf
./hsh
.fi

The shell displays a prompt and waits for the user to enter a command.

Example:

.nf
$ ./hsh
($) /bin/ls
($) exit
.fi

.SH "NON-INTERACTIVE MODE"
The shell can also receive commands through standard input.

Example:

.nf
echo "/bin/ls" | ./hsh
.fi

.SH "EXIT STATUS"
The shell returns an exit status depending on the result of the executed
command or internal operation.

.SH EXAMPLES
Run the shell:

.nf
./hsh
.fi

Execute a command:

.nf
($) /bin/ls
.fi

Run a command in non-interactive mode:

.nf
echo "/bin/ls" | ./hsh
.fi

.SH AUTHORS
Written by Leonid Kuryshev and contributors.

.SH "VERSION FRANCAISE"
Le shell peut etre utilise en mode interactif ou en mode non interactif.

.SH "MODE INTERACTIF"
Pour demarrer le shell en mode interactif :

.nf
./hsh
.fi

Le shell affiche une invite de commande et attend que l'utilisateur
entre une commande.

Exemple :

.nf
$ ./hsh
($) /bin/ls
($) exit
.fi

.SH "MODE NON INTERACTIF"
Le shell peut egalement recevoir des commandes depuis l'entree standard.

Exemple :

.nf
echo "/bin/ls" | ./hsh
.fi

.SH "STATUT DE SORTIE"
Le shell retourne un statut de sortie en fonction du resultat de la
commande executee ou d'une operation interne.

.SH EXEMPLES
Lancer le shell :

.nf
./hsh
.fi

Executer une commande :

.nf
($) /bin/ls
.fi

Executer une commande en mode non interactif :

.nf
echo "/bin/ls" | ./hsh
.fi

.SH AUTEURS
Ecrit par Leonid Kuryshev et les contributeurs.